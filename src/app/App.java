package app;

import app.controller.GraphController;

public class App {
    public static void main(String[] args) {
        GraphController controller = new GraphController();
        controller.runApp();
    }
}
